class Group{
  String groupId;
  String eventId;
  String groupName;
  String groupImage;
  List<dynamic> members;

  Group({required this.groupId, required this.eventId, required this.groupName,required this.groupImage, required this.members});
}